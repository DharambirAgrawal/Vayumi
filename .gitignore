# Secrets — never commit (use *.example templates for templates)
.env
server/.env
client/browser/.env
.env.local
server/.env.local
client/browser/.env.local
*.env.local

.env.prod


# Server-local: SQLite, ChromaDB, downloaded ML weights (SpeechBrain, etc.)
server/data/
server/models/

__pycache__/
*.pyc

venv
data

models

node_modules
dist 
package-lock.json